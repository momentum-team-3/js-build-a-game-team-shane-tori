let canvas = document.querySelector ("#canvas")

function getContext () {
    return canvas.getContext("2d")
}

function makeObject (x,y, velocity, size, image) {
       let object = {     x: x,       y: y,    
         image: image,        velocity: velocity.      
          size: size,      image: image;   }   
          return object}


function rabbit (x,y, width, height, )
function rabbit (x,y, width, height)
function rabbit (x,y, width, height)
function rabbit (x,y, width, height)
function rabbit (x,y, width, height)


